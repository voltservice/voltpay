# ==========================================
# JVM & Memory Optimization
# ==========================================
org.gradle.jvmargs=-Xmx8G -Xms4G \
-XX:MaxMetaspaceSize=1G \
-XX:+UseG1GC \
-XX:+UseStringDeduplication \
-XX:+HeapDumpOnOutOfMemoryError \
-XX:HeapDumpPath=./heapdumps \
-Dfile.encoding=UTF-8 \
-Dkotlin.daemon.jvm.options=-Xmx4G

# ==========================================
# Build Performance Tweaks
# ==========================================
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.caching=true
org.gradle.workers.max=8

# ==========================================
# AndroidX & Jetifier
# ==========================================
android.useAndroidX=true
android.enableJetifier=true
# ==========================================
# Flutter Integration
# ==========================================
flutter.buildMode=release
# ==========================================
# Java Home Explicit Path (already set but keeping for clarity)
# ==========================================
org.gradle.java.home=C\:\\Program Files\\Java\\jdk-21
# ==========================================
# Warning Suppressions (Optional - Only if necessary)
# ==========================================
# Disables obsolete compiler option warnings (only if absolutely necessary)
# org.gradle.warning.mode=none
# ==========================================
RELEASE_STORE_FILE=C\:\\Users\\Metal\\voltpay-release.keystore
RELEASE_STORE_PASSWORD=E06cH!&pKxipgeNr
RELEASE_KEY_ALIAS=voltpay
RELEASE_KEY_PASSWORD=E06cH!&pKxipgeNr